package checker.rules;

import execute.data.RunData;
import gui.MainFrame;

import java.util.List;

// bulk_import rule -> validating columns before import
public class Rule01 extends AbstractRule{
    @Override
    public boolean checkRule(Object data) {
//        RunData runData = (RunData) data;
//        List<String> realColumns;
//        MainFrame.getInstance().getJTree().getSelectionModel().getLast
        return true;
    }
}
